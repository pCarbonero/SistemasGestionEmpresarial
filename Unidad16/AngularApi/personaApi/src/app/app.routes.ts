import { Routes } from '@angular/router';
import { TablaPersonasComponent } from './components/tabla-personas/tabla-personas.component';

export const routes: Routes = [
    {
        path: 'tablapersonas',
        component: TablaPersonasComponent
    }
];
